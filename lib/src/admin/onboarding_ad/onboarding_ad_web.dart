import 'package:flutter/material.dart';

class OnboardingAdWeb extends StatelessWidget {
  const OnboardingAdWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
