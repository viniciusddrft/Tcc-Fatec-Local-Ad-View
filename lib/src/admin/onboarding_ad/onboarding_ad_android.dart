import 'package:flutter/material.dart';

class OnboardingAdAndroid extends StatelessWidget {
  const OnboardingAdAndroid({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
